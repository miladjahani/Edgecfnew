# راهنمای استقرار و استفاده از پنل مدیریت Miliconfig Pro

## اطلاعات ورود ادمین اصلی
- **ایمیل:** milad201400@gmail.com
- **رمز عبور:** 13781378mM@@

## مراحل نصب و راه‌اندازی

### 1. اجرای مایگریشن‌های دیتابیس
به پنل Supabase بروید و فایل‌های migration زیر را به ترتیب اجرا کنید:

```bash
supabase/migrations/20260729113444_create_miliconfig_pro_schema.sql
supabase/migrations/20260729114843_add_deployment_config_columns.sql
supabase/migrations/20260729115557_add_deployment_logs_column.sql
supabase/migrations/20260729120000_add_admin_and_support.sql
supabase/migrations/20260729130000_add_support_messages.sql
supabase/migrations/20260729140000_ensure_admin_setup.sql
supabase/migrations/20260729150000_add_banned_users_view.sql
```

یا از SQL Editor در پنل Supabase استفاده کنید و محتوای هر فایل را کپی و اجرا کنید.

### 2. دیپلوی Edge Function ربات تلگرام
```bash
cd /workspace
npx supabase functions deploy telegram-bot
```

### 3. پیکربندی ربات تلگرام
1. وارد پنل مدیریت شوید با ایمیل `milad201400@gmail.com`
2. به بخش "ربات تلگرام" بروید
3. توکن ربات خود را از @BotFather دریافت و وارد کنید
4. پیام خوش‌آمدگویی را تنظیم کنید
5. دکمه "ذخیره تنظیمات" را بزنید

## ویژگی‌های جدید پنل مدیریت

### 🔐 دسترسی‌های ادمین اصلی (milad201400@gmail.com)
- نمایش نشان تاج 👑 در کنار نام کاربری
- دسترسی کامل به تمام بخش‌ها
- مدیریت ادمین‌های دیگر
- ارسال پیام همگانی به کاربران ربات
- مشاهده گزارش فعالیت‌های سیستم

### 👥 مدیریت کاربران ربات
- **لیست کاربران:** مشاهده تمام کاربران با فیلتر و جستجو
- **آمار کاربران:** کل، فعال، ادمین‌ها، کاربران امروز
- **عملیات روی کاربران:**
  - ✅ فعال/غیرفعال کردن
  - 👑 ارتقا به ادمین یا حذف دسترسی ادمین
  - 🗑 حذف کاربر از دیتابیس
  - 🔍 جستجو بر اساس نام، یوزرنیم یا آیدی تلگرام
  - ⛔ مشاهده کاربران مسدود شده

### 📞 پشتیبانی
- **تب پشتیبانی** در منوی اصلی اضافه شد
- لینک کانال تلگرام: https://t.me/miliconfig
- تماس مستقیم با ادمین: https://t.me/milad201400
- سیستم تیکت پشتیبانی
- سوالات متداول (FAQ)

### 🤖 ربات تلگرام پیشرفته

#### دستورات جدید:
- `/start` - شروع کار با ربات و نمایش منوی اصلی
- `/support` - دریافت لینک پشتیبانی
- `/makeadmin @username` - تعیین کاربر به عنوان ادمین (فقط ادمین اصلی)
- `/removeadmin @username` - حذف دسترسی ادمین (فقط ادمین اصلی)
- `/broadcast متن پیام` - ارسال پیام همگانی (فقط ادمین اصلی)
- `/help` - نمایش راهنمای کامل با دکمه‌های شیشه‌ای

#### منوی دکمه‌های شیشه‌ای:
- 🚀 استقرار ورکر
- 📊 وضعیت سرویس‌ها
- 📋 ورکرها
- 🔗 کانفیگ‌ها
- 👥 مدیریت کاربران (فقط ادمین‌ها)
  - 📊 آمار کاربران
  - 👥 لیست کاربران
  - 🔍 جستجوی کاربر
  - ⛔ کاربران مسدود
- 🔐 پنل ادمین (فقط ادمین‌ها)
  - 👤 مدیریت ادمین‌ها
  - 📊 گزارش فعالیت‌ها
  - ⚙️ تنظیمات سیستم (فقط ادمین اصلی)
  - 📢 ارسال پیام همگانی (فقط ادمین اصلی)
- 📞 پشتیبانی
- ❓ راهنما

#### ویژگی‌های ربات:
- ✅ احراز هویت واقعی بر اساس دیتابیس
- ✅ تشخیص ادمین اصلی از طریق ایمیل
- ✅ منوی تو در تو با دکمه‌های شیشه‌ای
- ✅ امکان ویرایش پیام‌ها
- ✅ لاگ‌گیری کامل از عملیات
- ✅ محدودیت دسترسی بر اساس سطح کاربر

## ساختار فایل‌های پروژه

```
/workspace
├── src/
│   ├── pages/
│   │   ├── Dashboard.tsx      # داشبورد اصلی با آمار
│   │   ├── BotUsers.tsx       # مدیریت کاربران ربات
│   │   ├── Support.tsx        # صفحه پشتیبانی
│   │   ├── BotConfig.tsx      # پیکربندی ربات
│   │   └── ...
│   ├── components/
│   │   └── Layout.tsx         # قالب اصلی با منو
│   └── lib/
│       ├── auth.tsx           # سیستم احراز هویت
│       └── types.ts           # تایپ‌ها
├── supabase/
│   ├── functions/
│   │   └── telegram-bot/      # Edge Function ربات
│   └── migrations/            # فایل‌های migration
└── dist/                      # خروجی بیلد
```

## نکات مهم

1. **امنیت:** فقط ایمیل `milad201400@gmail.com` دسترسی کامل به عنوان ادمین اصلی دارد
2. **ادمین‌ها:** می‌توانید کاربران دیگر را با دستور `/makeadmin` به عنوان ادمین تعیین کنید
3. **لاگ‌ها:** تمام عملیات ادمین‌ها در جدول `activity_logs` ذخیره می‌شود
4. **پشتیبانی:** کاربران می‌توانند از طریق تب پشتیبانی یا ربات تلگرام با شما در ارتباط باشند

## عیب‌یابی

### ربات تلگرام کار نمی‌کند:
1. بررسی کنید توکن ربات صحیح است
2. وب‌هوک را دوباره تنظیم کنید
3. لاگ‌های Edge Function را در پنل Supabase بررسی کنید

### کاربران نمی‌توانند وارد شوند:
1. بررسی کنید حساب کاربری در Supabase Auth ساخته شده
2. رمز عبور حداقل ۶ کاراکتر باشد
3. اتصال به اینترنت برقرار باشد

### پنل مدیریت نمایش داده نمی‌شود:
1. مرورگر را رفرش کنید (Ctrl+F5)
2. کش مرورگر را پاک کنید
3. کنسول مرورگر را برای خطاها بررسی کنید

---
**توسعه یافته توسط Miliconfig Pro**
