# ✅ قابلیت کاربران زیرمجموعه (Sub-Users)

## 📋 خلاصه ویژگی‌ها

این امکان به هر کاربر می‌دهد که با استفاده از توکن‌های خود، چندین کاربر زیرمجموعه با محدودیت‌های اختصاصی ایجاد کند.

### 🔑 ویژگی‌های اصلی:

1. **مدیریت چندکاربره**: هر توکن می‌تواند چندین کاربر زیرمجموعه داشته باشد
2. **محدودیت حجم ترافیک**: تعیین سقف مصرف برای هر کاربر (0 = نامحدود)
3. **محدودیت زمانی**: تعیین مدت اعتبار هر کاربر (0 = نامحدود)
4. **تنظیمات پنل اختصاصی**: 
   - شناسه Inbound جداگانه
   - تنظیم Flow (مثلاً xtls-rprx-vision)
   - نوع Security (none, tls, reality)
5. **وضعیت لحظه‌ای**: نمایش وضعیت فعال/غیرفعال و آنلاین بودن
6. **نوار پیشرفت ترافیک**: نمایش گرافیکی مصرف ترافیک

## 🗄️ دیتابیس

### جدول `sub_users`:

```sql
CREATE TABLE sub_users (
    id UUID PRIMARY KEY,
    token_id UUID REFERENCES tokens(id),
    username TEXT NOT NULL,
    password TEXT NOT NULL,
    traffic_limit_gb NUMERIC DEFAULT 0,
    traffic_used_gb NUMERIC DEFAULT 0,
    time_limit_days INTEGER DEFAULT 0,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    expires_at TIMESTAMPTZ,
    is_active BOOLEAN DEFAULT true,
    is_online BOOLEAN DEFAULT false,
    panel_settings JSONB,
    xui_user_id INTEGER,
    UNIQUE(token_id, username)
);
```

### امنیت (RLS):
- فقط صاحب توکن می‌تواند کاربران زیرمجموعه خود را مدیریت کند
- حذف خودکار هنگام حذف توکن والد (CASCADE)

## 📁 فایل‌های ایجاد شده:

| فایل | توضیح |
|------|-------|
| `supabase_sub_users_migration.sql` | اسکریپت ساخت جدول و سیاست‌های امنیتی |
| `src/pages/SubUsers.tsx` | صفحه مدیریت کاربران زیرمجموعه |
| `src/components/ui/*.tsx` | کامپوننت‌های UI مورد نیاز |

## 🚀 راهنمای استفاده:

### ۱. اجرای Migration در Supabase:

```bash
# وارد Supabase Dashboard شوید
# به بخش SQL Editor بروید
# محتوای فایل supabase_sub_users_migration.sql را کپی و اجرا کنید
```

### ۲. دسترسی به صفحه:

- از منوی سمت راست، گزینه **"کاربران زیرمجموعه"** را انتخاب کنید
- یا مستقیماً به آدرس `/sub-users` بروید

### ۳. افزودن کاربر جدید:

1. روی دکمه **"افزودن کاربر جدید"** کلیک کنید
2. توکن والد را انتخاب کنید
3. نام کاربری و رمز عبور را وارد کنید
4. محدودیت‌های ترافیک و زمان را تنظیم کنید
5. تنظیمات پنل اختصاصی را پیکربندی کنید
6. دکمه **"افزودن کاربر"** را بزنید

### ۴. مدیریت کاربران:

- **فعال/غیرفعال**: با استفاده از Switch
- **حذف**: با دکمه سطل آشغال
- **مشاهده مصرف**: نوار پیشرفت ترافیک
- **بررسی انقضا**: نمایش تاریخ انقضا و وضعیت

## 🎯 موارد استفاده:

- **فروشندگان VPN**: ایجاد اکانت‌های آزمایشی یا اشتراکی
- **مدیران تیم**:分配 محدودیت‌های مختلف به اعضای تیم
- **ارائه‌دهندگان خدمات**: فروش اشتراک‌های زمانی/حجمی

## ⚠️ نکات مهم:

1. **اتصال به X-UI**: این صفحه فقط اطلاعات را در دیتابیس ذخیره می‌کند. برای ایجاد واقعی کاربر در پنل X-UI، نیاز به پیاده‌سازی Backend دارید.

2. **مانیتورینگ مصرف**: فیلد `traffic_used_gb` و `is_online` باید توسط یک سرویس پس‌زمینه به‌روزرسانی شوند.

3. **امنیت**: رمزهای عبور باید به صورت encrypted ذخیره شوند.

## 🔧 توسعه‌های آینده:

- [ ] اتصال خودکار به API پنل X-UI
- [ ] تولید لینک اشتراک برای هر کاربر
- [ ] ارسال اطلاعیه هنگام نزدیک شدن به اتمام حجم/زمان
- [ ] گزارش‌گیری از مصرف کاربران
- [ ] امکان ویرایش کاربران موجود
- [ ] پشتیبانی از Import/Export

---

**ساخته شده برای Miliconfig Pro** ✨
